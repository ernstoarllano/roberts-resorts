import { extendTheme } from "@chakra-ui/core"

const theme = extendTheme({
  colors: {
    primary: {
      1: "#232f40",
      2: "#fe9c02",
      3: "#496387",
      4: "#355563",
    },
  },
})

export default theme
